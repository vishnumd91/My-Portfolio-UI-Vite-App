export type NavigationInputs = {
  id: number;
  heading: string;
};

export type SkillIconNames = {
  name: string;
};

export type WorkflowContent = {
  content: string;
};

export type EducationData = {
  subHeading: string;
  heading: string;
  course: string;
  grade: string;
  duration: string;
};
